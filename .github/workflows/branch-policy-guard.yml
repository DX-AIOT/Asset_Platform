name: Branch Policy Guard

on:
  pull_request:
    branches: [dev, main]
  workflow_dispatch:

jobs:
  pr-branch-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate PR branch policy
        run: ./scripts/branch-policy-guard.sh ci-pr
